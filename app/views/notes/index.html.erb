
<body>
<h1>R&J's<h1>
<h1>POST IT<h1>
<% @notes.each do |note| %>
    <p>
    <%= note.body %>
    <%= link_to("edit", edit_note_path(note.id))%>
    <%= link_to("delete", note_path(note.id), method: :delete, data: {confirm: "Are you really sure you want to delete this note?"}) %>
    </p>
<% end %>
</body>